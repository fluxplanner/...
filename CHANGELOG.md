# Changelog

## Unreleased

- Reading task type + dashboard **Reading** filter; calendar month label shows **week number**; iCal export includes **day-before VALARM** reminders
- Focus timer **25 / 45 / 90** presets; **Mood** one-line journal (saved per day); **Settings → Data** MLA citation helper, **local storage** estimate, **import encrypted** backup; **Grades** export CSV
- Command palette: export grades CSV, print, import encrypted backup
- Improvement catalog (`IMPROVEMENTS.md`) with status for 100+ ideas
- Task snooze, bulk select, weekly repeat on complete, “waiting on” field
- Exam conflict banner (multiple tests/quizzes same day)
- Font size & reduce motion (Settings → Theme)
- Encrypted JSON backup (Web Crypto AES-GCM)
- Quick-add history (datalist suggestions)
- FAQ in Settings; skip link; toast live region; print-friendly CSS hooks

## Earlier

- See git history on `main` for prior releases.
